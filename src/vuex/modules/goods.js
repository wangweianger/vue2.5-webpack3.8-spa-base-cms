import * as types from '../mutation-types'

const state = {
    allGoodsDetail: {}, // 商品详情 Key为itemCode
}

const mutations = {
    
}

const actions = {
   
}

export default {
    state,
    mutations,
    actions,
}
